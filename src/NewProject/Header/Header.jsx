import React from 'react';
import s from './Header.module.css';

const Header = () => {
    return (
        <div className={s.Header}>
            <h1>React <code> this easy</code></h1>
        </div>
    );
};

export default Header;